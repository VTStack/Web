export enum Messages {
  'Already a member in group' = 'ALREADY_MEMBER',
  'No previews available' = 'NO_PREVIEWS_AVAILABLE',
  'User owns this invite' = 'USER_OWNS_INVITE',
  'Already exists' = 'ALREADY_EXISTS'
}
