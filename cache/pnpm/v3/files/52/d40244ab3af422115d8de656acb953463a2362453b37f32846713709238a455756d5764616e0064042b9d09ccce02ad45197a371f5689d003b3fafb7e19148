import { ReactNode } from 'react';
export interface ModalProps {
    isOpen: boolean;
    children?: ReactNode;
    width?: string;
    onClickAway?: () => void;
    gap?: string;
}
export declare function Modal({ isOpen, children, width, gap }: ModalProps): JSX.Element;
export default Modal;
