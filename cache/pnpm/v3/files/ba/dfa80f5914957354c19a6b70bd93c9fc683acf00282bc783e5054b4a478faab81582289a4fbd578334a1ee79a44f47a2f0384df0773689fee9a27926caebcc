import { useState } from 'react';

const useToggle = (initial = false) => {
  const [toggle, setToggle] = useState(initial);
  return [toggle, value => setToggle(val => value ? value : !val)];
};

export { useToggle };
