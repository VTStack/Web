import { BuildNodeBuilderOptions, NormalizedBuildNodeBuilderOptions } from './types';
export interface FileReplacement {
    replace: string;
    with: string;
}
export declare function normalizeBuildOptions(options: BuildNodeBuilderOptions, root: string, sourceRoot: string, projectRoot: string): NormalizedBuildNodeBuilderOptions;
