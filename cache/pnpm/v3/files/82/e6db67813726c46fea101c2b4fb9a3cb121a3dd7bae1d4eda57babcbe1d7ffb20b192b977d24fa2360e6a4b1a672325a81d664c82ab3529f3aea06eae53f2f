import { Hash, Hasher, NxJsonConfiguration, ProjectGraph, Task, TaskGraph, WorkspaceJsonConfiguration } from '@nrwl/devkit';
export default function run(task: Task, context: {
    hasher: Hasher;
    projectGraph: ProjectGraph;
    taskGraph: TaskGraph;
    workspaceConfig: WorkspaceJsonConfiguration & NxJsonConfiguration;
}): Promise<Hash>;
